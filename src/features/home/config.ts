export const homeConfig = {
  poster: "/media/whitespace-mountains.webp",
  mediaQuery: "(min-width: 1024px) and (hover: hover) and (pointer: fine) and (prefers-reduced-motion: no-preference)",
  bitmapLimit: 24,
  bitmapConcurrency: 4,
  loadTimeout: 8000,
  lightFrame: 168,
} as const;
