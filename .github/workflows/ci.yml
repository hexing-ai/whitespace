name: CI
on:
  push:
    branches: [main]
  pull_request:
permissions:
  contents: read
jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      NEXT_TELEMETRY_DISABLED: '1'
      DASHSCOPE_API_KEY: ''
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm run lint
      - run: npm test
      - run: npm run build
      - run: npx playwright install --with-deps chromium
      - run: node scripts/cache-demo-video.mjs "${{ runner.temp }}/whitespace-hero.mp4"
      - run: npm run test:e2e
        env:
          PLAYWRIGHT_CHANNEL: chromium
          WHITESPACE_VIDEO_FIXTURE: ${{ runner.temp }}/whitespace-hero.mp4
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: browser-failure
          path: test-results/browser/
          retention-days: 3
